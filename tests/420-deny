# $Id$

prompt "dispatch deny:        "

SENDER=userx@host
run ezmlm-make -+k "$DIR"
run ezmlm-sub "$DIR" deny "$SENDER"

dispatch \
&& fatal "did not deny denied address in editor after -k"

run ezmlm-make -+K "$DIR"

${ECHO} "OK"
