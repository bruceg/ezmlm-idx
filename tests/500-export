# $Id$

  SENDER="test@example.org"
  ${ECHO} "From: Test <${SENDER}>" >"$TMP"

  ${ECHO} Verifying message header and body contents...

  ${EZBIN}/ezmlm-make -+ -S "$DIR" || \
  fatal "ezmlm-make failed"
