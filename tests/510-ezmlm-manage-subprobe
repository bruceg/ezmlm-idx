# $Id$

  prompt "subscribe probe:      "

  DEFAULT="subscribe"
  LOCAL="${LOC}-${DEFAULT}"
  file_run ezmlm-manage "$DIR" || \
  fatal "failed to produce subscribe probe"

  grephdr_manage
  grephdr reply-to: "${LOC}-sc\\..*\\..*-test=example.org@${HOST}"
  grephdr subject: "CONFIRM subscribe to ${LOC}@${HOST}"
  grephdr_empty

  ${ECHO} OK
