# $Id$

  prompt "unsubscribe:          "

  DEFAULT="unsubscribe"
  LOCAL="${LOC}-${DEFAULT}"
  file_run ezmlm-manage -U "$DIR" || \
  fatal "failed to produce unsubscribe confirmation"

  grephdr_manage
  grephdr subject: "goodbye from ${LOC}@${HOST}"
  grephdr_empty

  ${ECHO} OK
